{
  "name": "meeting-facilitator",
  "version": "1.0.0",
  "type": "module",
  "description": "Real-time meeting facilitation app for gathering and revealing team responses",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"wait-on http://localhost:3001/api/health && npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "lint": "eslint client/src server/src",
    "lint:fix": "eslint client/src server/src --fix",
    "format": "prettier --write \"client/src/**/*.{ts,tsx}\" \"server/src/**/*.ts\"",
    "format:check": "prettier --check \"client/src/**/*.{ts,tsx}\" \"server/src/**/*.ts\"",
    "typecheck": "npm run typecheck:server && npm run typecheck:client",
    "typecheck:server": "cd server && npm run typecheck",
    "typecheck:client": "cd client && npx tsc --noEmit",
    "knip": "knip",
    "knip:fix": "knip --fix",
    "check:all": "npm run typecheck && npm run lint && npm run format:check && npm run knip",
    "fix:all": "npm run lint:fix && npm run format && npm run knip:fix",
    "test:e2e": "OPENAI_BASE_URL=http://localhost:8080/v1 playwright test",
    "test:e2e:ui": "OPENAI_BASE_URL=http://localhost:8080/v1 playwright test --ui",
    "test:e2e:headed": "OPENAI_BASE_URL=http://localhost:8080/v1 playwright test --headed",
    "test:e2e:debug": "OPENAI_BASE_URL=http://localhost:8080/v1 playwright test --debug",
    "test:e2e:report": "playwright show-report",
    "test:e2e:wiremock": "npm run wiremock:start && wait-on http-get://localhost:8080/__admin/ && npm run test:e2e; npm run wiremock:stop && npm run test:e2e:cleanup",
    "test:e2e:wiremock:ui": "npm run wiremock:start && wait-on http-get://localhost:8080/__admin/ && npm run test:e2e:ui; npm run wiremock:stop && npm run test:e2e:cleanup",
    "wiremock:start": "docker compose -f docker-compose.e2e.yml up -d",
    "wiremock:stop": "docker compose -f docker-compose.e2e.yml down",
    "wiremock:logs": "docker compose -f docker-compose.e2e.yml logs -f",
    "test:e2e:cleanup": "npx tsx e2e/global-teardown.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@playwright/test": "^1.57.0",
    "@supabase/supabase-js": "^2.88.0",
    "concurrently": "^8.2.2",
    "dotenv": "^17.2.3",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "knip": "^5.73.3",
    "prettier": "^3.7.4",
    "tsx": "^4.21.0",
    "typescript-eslint": "^8.49.0",
    "wait-on": "^9.0.3"
  }
}
